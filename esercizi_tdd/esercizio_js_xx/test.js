import { func_name } from './es_xx.js';

describe("ESERCIZIO xxx", () => {
    test('TEST 01', () => {
        expect(func_name()).toBe();
    });

    test('TEST 02', () => {
        expect(func_name()).toBe();
    });

    test('TEST 03', () => {
        expect(func_name()).toBe();
    });

    test('TEST 04', () => {
        expect(func_name()).toBe();
    });

    test('TEST 05', () => {
        expect(func_name()).toBe();
    });
});

